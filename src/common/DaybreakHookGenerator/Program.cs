using System;

using Terraria.ModLoader;

namespace DaybreakHookGenerator;

internal static class Program
{
    public static void Main()
    {
        Console.WriteLine(typeof(ModSystem).FullName);
    }
}